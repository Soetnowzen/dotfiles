#!/bin/tcsh

set autoexpand

alias ls ls -F --color --group-directories-first
alias la ls -A
alias ll la -l
alias :q exit
alias find find \!* | grep '[^/]*$'
alias g git
alias grep grep --color
alias grepc grep -Rin --color --include=*.{cc,h}
alias less less -r
alias print_path 'echo $PATH | tr : "\n"'
alias g_pl_stash 'git stash && git pull && git stash pop'
alias tm 'tmux attach || tmux new'
alias v vim
alias vimr vim ~/.vimrc
alias v-split vim -o
alias v-vsplit vim -O
alias v-tsplit vim -p

set color
setenv LS_COLORS "di=0;35:ln=0;36:ex=0;33:pi=0;32:so=0;31:bd=0;37:mi=0;36:cd=1;35:tw=0;30:ow=0;34:"
setenv TERM xterm-256color

# alias color_failure grep -i \'err\\w*\\\|warn\\w*\\\|info\\w*\\\|fail\\w*\\\|\$\'

alias getCurrentGitBranch 'git branch |& grep \* | sed "s/\* \(.*\)/(\1)/"'
alias precmd set 'prompt = "%{\033[36m%}%T %{\033[34m%}%n@%m%{\033[0m%}:%{\033[32m%}%~%{\033[33m%}`getCurrentGitBranch`%{\033[0m%}%# "'

# bindkey -v

# Autocomple searches when using up and down
bindkey "^[[A" history-search-backward
bindkey "^[[B" history-search-forward

if (-r ~/.cshrc.work) source ~/.cshrc.work

